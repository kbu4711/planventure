info:
  name: RegisterNewUser
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:5000/api/auth/register
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "bruno.banani@email.com",
        "password": "SecurePass123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
